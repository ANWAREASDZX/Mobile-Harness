Sovereign proxy: API keys no longer leave the app vault (not visible inside the Linux guest). One shared output tailer cuts battery drain from polling, session-scoped wake locks and thermal-throttle visibility. Verified agent updates (pinned digests), project toolchain assets moved to the project release channel, storage guards for downloads and checkpoints, GitHub sign-in tokens moved to the hardware-backed vault. New: THIRD_PARTY_NOTICES, SBOM in CI, file-size guard.
