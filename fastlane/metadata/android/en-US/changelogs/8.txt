Conversations now live in a bounded SQLite store instead of JSON files rewritten on every agent event: faster saves, far less flash wear, and hard caps per chat (2,000 messages) and per message. One-time verified import keeps every existing chat. Terminal history restored from disk is scrubbed of credential-shaped values (API keys, tokens, passwords) before saving. Chats per project are capped at 100, oldest first out.
